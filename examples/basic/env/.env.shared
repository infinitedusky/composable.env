# Shared team values — commit this file (no secrets, or use with caution)
# Override anything here in env/.env.local (gitignored)

DATABASE_PROD_HOST=db.example.com
DATABASE_STAGING_HOST=db-staging.example.com
