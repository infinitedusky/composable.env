{
  "name": "Docker",
  "description": "Docker Compose environment — uses internal Docker networking hostnames",
  "components": {
    "database": ["default", "docker"],
    "redis": ["default", "docker"]
  }
}
